import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '04_pokedex_app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
